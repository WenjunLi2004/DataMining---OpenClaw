# OpenClaw API 配置模板
# 复制此文件为 .env，填入你的 Key，不要提交 .env 到 git
#
# ── 选项 A：直接用 DeepSeek ──────────────────────────────────────
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_BASE_URL=https://api.deepseek.com
# OPENCLAW_INSIGHT_MODEL=deepseek-chat
#
# ── 选项 B：用 OpenRouter（推荐，支持多模型切换）──────────────────
# DEEPSEEK_API_KEY=sk-or-v1-...
# DEEPSEEK_BASE_URL=https://openrouter.ai/api/v1
# OPENCLAW_INSIGHT_MODEL=deepseek/deepseek-chat
#
# ── 其他可用的 OpenRouter 模型（改 OPENCLAW_INSIGHT_MODEL 即可）──
# OPENCLAW_INSIGHT_MODEL=qwen/qwen-plus          # 阿里通义千问
# OPENCLAW_INSIGHT_MODEL=google/gemini-flash-1.5 # Google Gemini
# OPENCLAW_INSIGHT_MODEL=anthropic/claude-3-haiku # Claude Haiku
#
# ── 注意 ─────────────────────────────────────────────────────────
# --with-embeddings 功能需要 deepseek-embedding 模型，
# 该模型 OpenRouter 不支持，必须用 DeepSeek 直连。
# 其余功能（error-analyst、insight-analysis）均支持 OpenRouter。
